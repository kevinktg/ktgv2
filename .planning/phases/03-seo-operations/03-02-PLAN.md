---
phase: 03-seo-operations
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01-PLAN.md"
files_modified:
  - .planning/phases/03-seo-operations/03-02-PLAN.md
  - src/app/sitemap.js
  - vercel.json
  - package.json
  - .github/workflows/*.yml
autonomous: true
requirements:
  - SEO-03
  - OPS-01
  - OPS-02
user_setup: []

must_haves:
  truths:
    - "Sitemap includes core static routes (`/`, `/blog`, `/expertise`, `/validation`, `/hub`, `/hub/snippets`, `/hub/chat` as product requires) plus **all** blog post URLs the site should expose — scaling beyond WordPress default `per_page` (e.g. 100) via pagination or chunked generation."
    - "Documented install/build uses **pnpm** and matches CI; `pnpm run build` is the gate."
    - "Speed Insights (or documented equivalent) remains enabled for production monitoring story."
  artifacts:
    - path: "src/app/sitemap.js"
      provides: "Next.js MetadataRoute sitemap"
---

<objective>

**Plan 03-02:** **SEO-03** (sitemap at scale) + **OPS-01** (build/CI parity with pnpm) + **OPS-02** (Speed Insights verification).

</objective>

<execution_context>

@C:/Users/kevin/.codex/get-shit-done/workflows/execute-plan.md

</execution_context>

<context>

@.planning/phases/03-seo-operations/03-CONTEXT.md
@src/lib/wordpress.js
@vercel.json
@package.json

</context>

## Task 1 — Implement `sitemap.js`

<action>

Add `src/app/sitemap.js` (or `.ts` if project adds TS later — **prefer `.js`** to match repo). Export default async function returning `{ url, lastModified?, changeFrequency?, priority? }[]`:

- Static entries for marketing/hub routes that should be indexed (trim if `robots`/product says no).
- **Blog posts:** Use `getPosts` in a loop or with increased page size until empty — must not silently cap at 100 if product requires full catalog (mirror `blog/page.jsx` pagination strategy).

Respect `metadataBase` / site URL: `https://ktg.one` or `process.env.NEXT_PUBLIC_SITE_URL`.

</action>

<verify>

<automated>pnpm run build && node -e "fetch('http://localhost:3000/sitemap.xml').then(r=>r.text()).then(console.log)"</automated>

</verify>

<done>Note: automated fetch requires dev server — or inspect build output / production URL.</done>

---

## Task 2 — OPS-01 CI / docs parity

<action>

- If `.github/workflows/*` exists: ensure install uses `pnpm` (`pnpm/action-setup` + `pnpm install --frozen-lockfile`); build command `pnpm run build`.
- If no CI: append short **Build** subsection to `CLAUDE.md` or `.planning/STATE.md` noting **pnpm** as canonical (roadmap still mentions npm legacy — optional one-line reconciliation).

</action>

<verify>

<automated>pnpm run build</automated>

</verify>

<done>No contradictory `npm run build` as only documented gate without pnpm note.</done>

---

## Task 3 — OPS-02 Speed Insights

<action>

Confirm `@vercel/speed-insights/next` usage in `layout.jsx` is correct for App Router. If behind env flag, document `NEXT_PUBLIC_VERCEL_ENV` or Vercel-only behaviour in SUMMARY.

</action>

<verify>

<automated>pnpm run build</automated>

</verify>

<done>SUMMARY states production monitoring expectation.</done>

---

<verification>

- `pnpm run lint && pnpm run build`
- Production or preview: open `/sitemap.xml` — valid XML, expected URLs

</verification>

<success_criteria>

SEO-03 + OPS-01 + OPS-02 closed or explicitly waived with reason in `03-02-SUMMARY.md`.

</success_criteria>
