---
phase: 03-seo-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-seo-operations/03-01-PLAN.md
  - src/app/layout.jsx
  - src/app/page.jsx
  - src/app/blog/page.jsx
  - src/app/blog/[slug]/page.jsx
  - src/app/expertise/page.jsx
  - src/app/validation/page.jsx
  - src/app/hub/snippets/page.jsx
autonomous: true
requirements:
  - SEO-01
  - SEO-02
user_setup: []

must_haves:
  truths:
    - "Every primary public route in scope exports route-level `metadata` or inherits root with an intentional default; no blank `<title>` / missing description on `/`, `/blog`, `/blog/[slug]`, `/expertise`, `/validation`."
    - "Open Graph and Twitter card image URLs are **absolute** (https://…) for dynamic `generateMetadata` paths; static metadata uses paths that resolve via root `metadataBase`."
    - "`pnpm run lint` and `pnpm run build` exit 0 after edits."
  artifacts:
    - path: "src/app/layout.jsx"
      provides: "metadataBase + default OG"
    - path: "src/app/blog/[slug]/page.jsx"
      provides: "generateMetadata + OG/twitter images"
---

<objective>

**Plan 03-01:** Audit and complete **SEO-01** (titles/descriptions) and **SEO-02** (OG/Twitter **absolute** images) across marketing shell and blog templates.

</objective>

<execution_context>

@C:/Users/kevin/.codex/get-shit-done/workflows/execute-plan.md

</execution_context>

<context>

@.planning/phases/03-seo-operations/03-CONTEXT.md
@.planning/REQUIREMENTS.md
@src/app/layout.jsx
@src/app/blog/page.jsx
@src/app/blog/[slug]/page.jsx

</context>

## Task 1 — Inventory routes

<action>

List App Router segments under `src/app/` that render **public HTML** (exclude `api/`, `_` private folders). For each: note whether it exports `metadata` / `generateMetadata` or relies on root layout only.

</action>

<verify>

<automated>rg -n "export (const|async function) metadata|generateMetadata" src/app --glob "*.jsx"</automated>

</verify>

<done>Table in SUMMARY: route → metadata source.</done>

---

## Task 2 — Homepage + static marketing pages

<action>

- **`/`** (`page.jsx`): If no `metadata` export, add minimal `export const metadata = { title, description }` **or** document in code comment why root layout defaults are sufficient for SEO-01.
- **`/expertise`**, **`/validation`**, **`/hub/snippets`** (and `/hub` redirect target if applicable): ensure each has explicit `metadata` where missing; descriptions lowercase brand style per `AGENTS.md`.

</action>

<verify>

<automated>pnpm run build</automated>

</verify>

<done>Build green; each listed route has defined title/description strategy.</done>

---

## Task 3 — Blog index + post OG/Twitter hardening

<action>

- **`blog/page.jsx`:** Add `twitter` card metadata aligned with `openGraph` (summary_large_image or summary). Add `openGraph.images` if missing — use absolute URL via `metadataBase` pattern or `new URL('/assets/og-image.jpg', metadataBase)`.
- **`blog/[slug]/page.jsx`:** In `generateMetadata`, ensure `featuredImage` and any `images[]` entries are **absolute**: if `getFeaturedImage` returns a path without protocol, prefix with `siteUrl` (same pattern as JSON-LD in file). Double-check `twitter.images` matches OG.

</action>

<verify>

<automated>pnpm run lint && pnpm run build</automated>

</verify>

<done>No relative `http`-ambiguous image strings in returned metadata for posts.</done>

---

## Task 4 — Root layout cross-check

<action>

Confirm `metadataBase` remains `https://ktg.one` (or env-driven documented pattern). If `openGraph.images` uses relative paths, confirm Next resolves them (per Next.js Metadata docs); if any consumer strips `metadataBase`, switch to explicit absolute URLs for images.

</action>

<verify>

<automated>pnpm run build</automated>

</verify>

<done>Document in SUMMARY: SEO-02 compliance statement.</done>

---

<verification>

- `pnpm run lint`
- `pnpm run build`
- Optional: View Source or `curl -s` one `/blog/[slug]` HTML for `og:image` / `twitter:image` hrefs — must be `https://…`

</verification>

<success_criteria>

SEO-01 and SEO-02 satisfied for audited routes; evidence in `03-01-SUMMARY.md`.

</success_criteria>
