# Plan 01-02 — Shell, hydration, and motion QA

**Phase:** 1 — Marketing shell & motion  
**Status:** Not started  
**Requirements:** MRKT-01, MRKT-02 (remaining)

## Goal

Confirm the global App Router shell, `ClientLayout`, and route transitions behave correctly with no hydration warnings on primary marketing routes, and that motion does not block content for users who prefer reduced motion.

## Tasks

- [ ] Walk primary routes (`/`, `/blog`, `/expertise`, `/validation`) and record any hydration or console errors.
- [ ] Validate `PageTransition` + Lenis + ScrollTrigger refresh ordering after navigation (no stuck pins).
- [ ] Add or verify `prefers-reduced-motion` handling for the heaviest GSAP sections (at minimum document gaps in `CONCERNS.md` if not implemented).
- [ ] Optional: audit `console.warn` in `ValidationSection` for production noise.

## Verification

- Manual pass on Chrome + one mobile viewport.
- No regressions in `npm run build`.

## Success criteria

- MRKT-01 / MRKT-02 acceptance language in `.planning/REQUIREMENTS.md` is satisfied or explicitly deferred with rationale.
