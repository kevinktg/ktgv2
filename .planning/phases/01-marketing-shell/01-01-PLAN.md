# Plan 01-01 — GSAP + Lenis correctness

**Phase:** 1 — Marketing shell & motion  
**Status:** In progress (code fixes landed in repo)  
**Requirements:** MOTN-01, MOTN-02 (partial MRKT-02)

## Goal

Align all scroll and timeline code with `@gsap/react` and GSAP 3 expectations: scoped `useGSAP`, valid ScrollTrigger options, ESM `ScrollTrigger` in the Lenis bridge, and dependency arrays when session/`hasPlayed` gates animations.

## Tasks

- [x] Replace `require("gsap/ScrollTrigger")` in `src/libs/lenis.jsx` with static `import` + `gsap.registerPlugin(ScrollTrigger)`.
- [x] Fix `SkipButton`: invalid `useGSAP(..., [])` — use `{ scope: buttonRef }`.
- [x] Remove duplicate horizontal wheel `useEffect` blocks in `BlogPreview.jsx`; keep a single handler.
- [x] Move `sessionStorage` marker to tween `onComplete` (ScrollTrigger config has no `onComplete`).
- [x] Add `dependencies: [hasPlayed]` (or equivalent) to `useGSAP` where `hasPlayed` / session gates timelines.

## Verification

- `npm run build` passes.
- Spot-check: hero/blog/expertise/validation sections on `/` with Lenis scrolling; no duplicate wheel capture on blog strip.

## Notes

- `CareerTimeline` uses animated `filter: blur()` — valid GSAP, but heavier than transform/opacity; consider a follow-up perf pass (not blocking this plan).
