---
plan_id: 02-01
phase: 2
wave: 1
depends_on: []
requirements_addressed: [CMS-01, CMS-02, CMS-03]
files_modified:
  - src/lib/wordpress.js
autonomous: true
---

# Plan 02-01 — WordPress client hardening

<objective>
Route all WordPress REST JSON reads through the existing timeout helper, keep the documented fetch policy (`no-store` vs `revalidate: 60`), and document the trust stance for `dangerouslySetInnerHTML` on post bodies (consumer: `src/app/blog/[slug]/page.jsx`).
</objective>

## must_haves

- `getPosts` and `getPostBySlug` primary code paths use **bounded-time** network I/O (same `REQUEST_TIMEOUT` as `fetchWithTimeout`).
- File header + explicit comment block for **HTML trust** (WordPress origin, admin responsibility, no new sanitizer unless separate task).
- `npm run build` succeeds.

---

## Task 1 — Unify fetch with timeout on list and slug

<action>
Refactor `src/lib/wordpress.js` so every `fetch` to `${WORDPRESS_URL}/wp-json/` for **posts** uses the shared `fetchWithTimeout` implementation (or a single internal `wordpressFetch(url, options)` that merges `AbortSignal`, preserves `cache` / `next` / `headers` from current call sites).

Concrete requirements:
- `getPosts`: replace the bare `fetch(url, { next: { revalidate: 60 }, headers: {...} })` with the timeout wrapper. Pass `signal` from `AbortController` inside `fetchWithTimeout` (existing pattern).
- `getPostBySlug`: same for the primary GET.
- Preserve **`next: { revalidate: 60 }`** on successful paths that currently use it.
- Preserve **403 fallback** behaviour: still attempt fallback URL without `_embed` using timeout-wrapped fetch.
- Keep `testWordPressConnection` as `cache: 'no-store'` per file header.

Do not change `WORDPRESS_URL` default or `REQUEST_TIMEOUT` value unless a bug is found; document if you must change ms.
</action>

<read_first>
- `src/lib/wordpress.js` (full file)
- `src/app/blog/[slug]/page.jsx` (lines containing `dangerouslySetInnerHTML` and imports from `@/lib/wordpress`)
- `.planning/PROJECT.md` — Constraints → WordPress fetch policy
</read_first>

<acceptance_criteria>
- `grep -n "fetch(" src/lib/wordpress.js` shows **no** direct `fetch(` to wp-json except inside `fetchWithTimeout` or a single new helper used by all post routes (zero duplicate unbounded `fetch` to posts API).
- `src/lib/wordpress.js` contains a comment block within 15 lines of `export async function getPostBySlug` OR at top of file that states: (1) post body HTML is **WordPress-published**, (2) trust boundary is **CMS/admin**, (3) app renders with `dangerouslySetInnerHTML` and **Tailwind prose** containment — verbatim phrases: `dangerouslySetInnerHTML`, `WordPress`, `trust`.
- `npm run build` exits 0 from repo root.
</acceptance_criteria>

<automated>
`npm run build`
</automated>

---

## Task 2 — Log surface consistency (non-breaking)

<action>
Where `getPosts` / `getPostBySlug` return `[]` or `null` due to **non-OK** HTTP or **abort**, ensure **one** `console.error` or `console.warn` line includes the string `WordPress` and the **HTTP status or `AbortError`** so support logs are grep-friendly. Do not throw to callers (preserve current return contracts).
</action>

<read_first>
- `src/lib/wordpress.js` after Task 1 edits
</read_first>

<acceptance_criteria>
- `grep -n "WordPress" src/lib/wordpress.js` includes at least one log path for failed response and one for timeout/abort if those paths exist.
- `npm run build` exits 0.
</acceptance_criteria>

<automated>
`npm run build`
</automated>

---

## PLANNING COMPLETE
