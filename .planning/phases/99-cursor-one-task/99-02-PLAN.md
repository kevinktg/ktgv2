---
phase: 99-cursor-one-task
plan: 02
type: execute
wave: 1
depends_on:
  - "99-01-PLAN.md"
files_modified:
  - .planning/phases/99-cursor-one-task/99-02-PLAN.md
  - .cursor/rules/MUSTDO.mdc
  - .planning/debug/mustdo-enforcement.md
  - .planning/phases/99-cursor-one-task/99-UAT.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - MOTN-01
user_setup: []

must_haves:
  truths:
    - "MUSTDO.mdc references the on-disk verification rule path (`.cursor/rules/verification-hard-gate.mdc`) with no broken merge lines."
    - "Every `node .cursor/scripts/*.js` referenced from `.cursor/hooks/cursor.json` exists and exits 0 when run from repo root."
    - "Phase 99 UAT (`99-UAT.md`) documents that scroll 'feel' claims require human or browser scroll evidence per verification-hard-gate — not lint/build alone."
  artifacts:
    - path: ".cursor/hooks/cursor.json"
      provides: "Hook run arrays"
    - path: ".planning/debug/mustdo-enforcement.md"
      provides: "Prior MUSTDO vs hooks diagnosis"
---

<objective>

**Plan-phase 99 — supplement:** Close the **MUSTDO ↔ hooks ↔ verification-hard-gate** loop so Phase 99 execution cannot silently skip evidence again.

**Depends on:** `99-01-PLAN.md` (transition QA) remains the feature plan; this plan is **process / discipline** only.

</objective>

<execution_context>

@C:/Users/kevin/.codex/get-shit-done/workflows/execute-plan.md

</execution_context>

<context>

@.planning/debug/mustdo-enforcement.md (ROOT CAUSE FOUND — hook drift, path casing)
@.cursor/rules/verification-hard-gate.mdc
@.cursor/hooks/cursor.json

</context>

<tasks>

<task type="auto">

<name>Task 1: MUSTDO + rule path hygiene</name>

<files>.cursor/rules/MUSTDO.mdc</files>

<action>

Ensure MUSTDO points at **`.cursor/rules/verification-hard-gate.mdc`** (exact on-disk name). Remove any merged `Read ...#嘘契約` line noise. Keep MCP items honest: **agent-invoked**, not hook-provable unless a future script calls APIs.

</action>

<verify>

<automated>rg -n "verification-hard-gate\\.mdc" .cursor/rules/MUSTDO.mdc</automated>

</verify>

<done>MUSTDO readable; path matches repo file.</done>

</task>

<task type="auto">

<name>Task 2: Hook script existence sweep</name>

<files>.cursor/hooks/cursor.json</files>

<action>

From repo root, for each distinct `node .cursor/scripts/...` in `cursor.json` `run` arrays: confirm file exists and `node <script>` exits **0**.

</action>

<verify>

<automated>node .cursor/scripts/verify-before-handoff.js</automated>

</verify>

<done>All referenced scripts present; handoff gate still lint+build.</done>

</task>

<task type="auto">

<name>Task 3: UAT note — evidence classes</name>

<files>.planning/phases/99-cursor-one-task/99-UAT.md</files>

<action>

Append a short **Evidence classes** subsection: (A) compile/lint/build, (B) SSR/DOM spot-check, (C) scroll/interaction — (C) required for Test 2–3 PASS per verification-hard-gate.

</action>

<verify>

<automated>test -f .planning/phases/99-cursor-one-task/99-UAT.md</automated>

</verify>

<done>UAT documents what automation can vs cannot prove.</done>

</task>

<task type="auto">

<name>Task 4: Debug log closure note</name>

<files>.planning/debug/mustdo-enforcement.md</files>

<action>

Append **Verification (2026-04-03):** MUSTDO path fixed; hook scripts verified; link to `99-02-PLAN.md`.

</action>

<verify>

<automated>grep -q "99-02-PLAN" .planning/debug/mustdo-enforcement.md</automated>

</verify>

<done>Debug file shows closure trail.</done>

</task>

</tasks>

<verification>

- `pnpm run lint` && `pnpm run build` after any doc/script edit touching runtime (if none, lint-only acceptable for markdown-only commits — project convention: still run lint when touching repo).

</verification>

<success_criteria>

MUSTDO aligned with disk; hooks cannot ENOENT; UAT explicitly separates proof classes; debug log updated.

</success_criteria>
