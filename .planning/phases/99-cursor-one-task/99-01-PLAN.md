---
phase: 99-cursor-one-task
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/99-cursor-one-task/99-01-PLAN.md
  - .planning/phases/99-cursor-one-task/99-UAT.md
  - .planning/ROADMAP.md
  - src/components/HeroTransition.jsx
  - src/components/ExpertiseTransition.jsx
  - src/libs/lenis.jsx
autonomous: false
requirements:
  - MOTN-01
user_setup: []

must_haves:
  truths:
    - "On `/`, HeroTransition scrub (clip-path wipe + grid reveal) advances smoothly across the full `h-dvh` band with no noticeable stall where animation finishes early while the band still scrolls (Lenis + ScrollTrigger stay in sync)."
    - "On `/`, ExpertiseTransition behaves the same (full-band scrub); white wipe and grid read clearly; bridge band stacks above pinned expertise shutters (`z-local-bridge` > `z-local-high`) without covering dock, skip, or cursors."
    - "`pnpm run lint` and `pnpm run build` exit 0 after any change."
    - "Manual UAT is recorded in `99-UAT.md` (pass/fail, browser, notes); ROADMAP Phase 99 progress reflects completion or documented gaps."
  artifacts:
    - path: ".planning/phases/99-cursor-one-task/99-UAT.md"
      provides: "Dated UAT log with pass/fail and reproduction notes"
    - path: "src/components/HeroTransition.jsx"
      provides: "Hero → Expertise band; ScrollTrigger start/end full passage"
      contains: 'start: "top bottom"'
    - path: "src/components/ExpertiseTransition.jsx"
      provides: "Expertise → Validation bridge; stacking + full-band ST"
      contains: 'z-local-bridge'
    - path: "src/libs/lenis.jsx"
      provides: "scrollerProxy + ticker-driven Lenis; lenis:scroll-ready refresh hook surface"
  key_links:
    - from: "src/libs/lenis.jsx"
      to: "ScrollTrigger"
      via: "scrollerProxy(window) + lenis scroll → ScrollTrigger.update"
    - from: "src/components/HeroTransition.jsx"
      to: "Lenis readiness"
      via: "lenis:scroll-ready listener + delayed ScrollTrigger.refresh"
    - from: "src/components/ExpertiseTransition.jsx"
      to: "ExpertiseSection pin"
      via: "Document order + z-local-bridge vs z-local-high in UI-STACKING-CONTRACT"
---

<objective>

Close **Phase 99** by **verifying** (and only if verification fails, **minimally fixing**) **HeroTransition** and **ExpertiseTransition** on `/` under the Lenis ↔ GSAP single-driver setup.

**Purpose:** Confirm homepage transition bands match planning intent (full-band scrub, readable wipes, correct local stacking vs pinned expertise) without regressing MOTN-01.

**Output:** Green lint/build, signed-off scroll feel in `99-UAT.md`, ROADMAP row updated; optional tiny diffs to transition files or `lenis.jsx` only if UAT fails.

</objective>

<execution_context>

@C:/Users/kevin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/kevin/.codex/get-shit-done/templates/summary.md

</execution_context>

<context>

@.planning/phases/99-cursor-one-task/99-CONTEXT.md
@.planning/ROADMAP.md (Phase 99 — Success Criteria)
@.planning/codebase/UI-STACKING-CONTRACT.md (§2–5 local vs app bands; pin + `z-local-bridge`)
@src/app/page.jsx (band order: Hero → HeroTransition → ExpertiseSection → ExpertiseTransition)
@src/lib/ktg-scroll-debug.js (`NEXT_PUBLIC_KTG_DEBUG_SCROLL=1` enables high-vis bands; markers also follow this module)

<interfaces>

**ScrollTrigger band contract (both components):** `trigger` = section root ref; `start: "top bottom"`; `end: "bottom top"`; `scrub` > 0; `invalidateOnRefresh: true`; `markers` from `ktgScrollMarkers`.

**QA hooks:** DOM ids `#hero-transition`, `#expertise-transition`; `?replay=1` clears session keys for replay (`HeroTransition` effect).

**Stacking (ExpertiseTransition):** Root uses `z-local-bridge` (46 per `globals.css`); shutters on pinned expertise remain `z-local-high` (45) — bridge must paint above shutters where they overlap, still below `z-dock` (52).

</interfaces>

</context>

## Goal-backward dependency order

1. **Automated gate** must pass before treating scroll QA as authoritative (broken build = stop).
2. **Human scroll verification** validates feel, stacking, and console health; records evidence.
3. **Fix-only-if-failed** runs only when Step 2 fails or Step 1 fails for a fixable reason; re-run Step 1 after edits.

## Risk and rollback

**Risk:** Tweaking `scrub`, ST `start`/`end`, or refresh timing can reintroduce “dead zone” or desync Lenis limit vs ST (`MOTN-01`). Raising `z-index` to “fix” overlap can violate `UI-STACKING-CONTRACT` and fight dock/modals.

**Rollback:** Keep changes minimal and reversible; if a fix feels worse, revert the single component (or `lenis.jsx`) to last committed state: `git checkout -- src/components/HeroTransition.jsx` (and/or `ExpertiseTransition.jsx`, `lenis.jsx`). Re-run `pnpm run lint` && `pnpm run build`.

<tasks>

<task type="auto">

<name>Task 1: Automated build gate + baseline doc</name>

<files>.planning/phases/99-cursor-one-task/99-UAT.md</files>

<action>

From repo root (`D:\projects\sites\ktgv2`):

1. Run `pnpm run lint` then `pnpm run build`; both must exit **0**.
2. Create or update `.planning/phases/99-cursor-one-task/99-UAT.md` with a **preflight** section: date, git short SHA (`git rev-parse --short HEAD`), `pnpm` version, Node if relevant — so human UAT correlates to a commit.

Do **not** add dependencies or touch DB. If lint or build fails **before** any Phase 99 code edit, fix only what blocks the gate (prefer unrelated fixes in a separate commit if scope creep).

</action>

<verify>

<automated>pnpm run lint && pnpm run build</automated>

</verify>

<done>Lint and build green; `99-UAT.md` exists with preflight metadata.</done>

</task>

<task type="checkpoint:human-verify" gate="blocking">

<name>Task 2: Manual scroll UAT — Hero + Expertise transitions</name>

<files>.planning/phases/99-cursor-one-task/99-UAT.md</files>

<action>Pause automation. Human runs dev server and executes every step in &lt;how-to-verify&gt; (scroll path, console, optional `NEXT_PUBLIC_KTG_DEBUG_SCROLL=1`, `?replay=1`). Do not edit source in this task — only observe and note PASS/FAIL for Task 3.</action>

<what-built>Executor has confirmed automated gate; no code change required for this checkpoint.</what-built>

<how-to-verify>

**Environment:** `pnpm dev` → open `http://localhost:3000/`.

1. **Console:** Open DevTools → Console. Scroll the full path: Hero → **HeroTransition** → Expertise (pinned section) → **ExpertiseTransition** → start of Validation. Note any GSAP/Lenis/React errors (ignore unrelated third-party noise only if clearly unrelated).
2. **HeroTransition (`#hero-transition`):** Scroll slowly through the band. Wipe should **track scroll** for the whole viewport-height band (no long segment where scroll moves but wipe/grid feel “finished” early). Grid is subtle unless debug is on — confirm wipe reads (dark slab recedes, grid strengthens) per design intent.
3. **ExpertiseTransition (`#expertise-transition`):** Same scrub check. Where the bridge overlaps pinned expertise UI, **bridge** must remain visually above shutters (`z-local-bridge` vs `z-local-high`); **DockNav** and cursor chrome must still read on top per contract (local band must not jump above dock).
4. **Optional debug:** Set `NEXT_PUBLIC_KTG_DEBUG_SCROLL=1` in `.env.local`, restart dev server. Confirm colored band tints and (per `ktg-scroll-debug`) marker behavior helps align triggers with `#hero-transition` / `#expertise-transition` geometry. Turn off after QA if desired.
5. **Replay:** Load `/?replay=1` once, then scroll Hero → HeroTransition again; confirm session replay behavior does not break transitions (no uncaught errors).

**Fail if:** dead-zone scrub, obvious z-order inversion (shutters over bridge where bridge should win; or transition covering dock), or repeating console errors tied to ScrollTrigger/Lenis.

</how-to-verify>

<verify>

<automated>MISSING — blocking human checkpoint; evidence captured in Task 3 via `99-UAT.md` and `pnpm run lint && pnpm run build` after any fix</automated>

</verify>

<done>Human returned **approved** or **fail** with repro per &lt;resume-signal&gt;.</done>

<resume-signal>Type **approved** if all checks pass; otherwise list **fail** with browser, steps, and whether a minimal code fix is requested.</resume-signal>

</task>

<task type="auto">

<name>Task 3: Record UAT, update ROADMAP, fix-only-if-failed</name>

<files>.planning/phases/99-cursor-one-task/99-UAT.md, .planning/ROADMAP.md, src/components/HeroTransition.jsx, src/components/ExpertiseTransition.jsx, src/libs/lenis.jsx</files>

<action>

**If Task 2 = approved:**

1. Append **Results** to `99-UAT.md`: PASS, checklist summary, browser used.
2. Update `.planning/ROADMAP.md` Phase 99 / progress table to reflect completion (per repo convention: checkboxes or status row).

**If Task 2 = failed:**

1. Document **FAIL** in `99-UAT.md` with repro and expected-vs-actual.
2. Apply **minimal** fix in `HeroTransition.jsx`, `ExpertiseTransition.jsx`, or `src/libs/lenis.jsx` only — no new packages, no schema. Prefer ST timing/stacking aligned with `99-CONTEXT.md` and `UI-STACKING-CONTRACT` (do not escalate z past app chrome).
3. Re-run `pnpm run lint && pnpm run build`.
4. Re-invite human confirmation (brief second pass or executor self-verify if policy allows); update `99-UAT.md` with outcome.
5. If still failing, leave documented gaps in `99-UAT.md` and **do not** mark ROADMAP complete — scope follow-up outside Phase 99 per CONTEXT boundary.

**Summary artifact:** If standard GSD flow applies, write `.planning/phases/99-cursor-one-task/99-01-SUMMARY.md` after execution.

</action>

<verify>

<automated>pnpm run lint && pnpm run build</automated>

</verify>

<done>`99-UAT.md` reflects final PASS or documented FAIL; ROADMAP updated only on PASS; any code change stays within the three allowed files unless preflight failure mandated elsewhere (avoid scope creep).</done>

</task>

</tasks>

<verification>

- Automated: `pnpm run lint` and `pnpm run build` succeed after all edits.
- Manual: Task 2 checklist completed; results in `99-UAT.md`.
- Stacking: Spot-check against `.planning/codebase/UI-STACKING-CONTRACT.md` (ExpertiseTransition remains local band; no accidental `z-*` above dock).

</verification>

<success_criteria>

Phase 99 is **done** when ROADMAP success criteria §1–§4 are satisfied: full-band scrub behavior and stacking on `/` for both transitions, green lint/build, and recorded manual UAT.

</success_criteria>

<output>

After completion: `.planning/phases/99-cursor-one-task/99-01-SUMMARY.md` (if using GSD summary convention).

</output>

---

## Nyquist verification — Phase 99 complete only if ALL are TRUE

- **N1 (Hero scrub):** Scroll position through `#hero-transition` maps monotonically to wipe/grid progress over the full `h-dvh` band (`top bottom` → `bottom top`), with Lenis smoothing — no large dead zone.
- **N2 (Expertise scrub):** Same for `#expertise-transition`.
- **N3 (MOTN-01):** No Lenis/ScrollTrigger desync visible in normal scroll (markers/debug optional evidence); no `[ReactLenis] Lenis instance never became available` in **production** path; dev bridge attaches as today.
- **N4 (Stacking):** Expertise bridge reads above pinned `z-local-high` shutters where they overlap; transition does not occlude `z-dock`, `z-skip`, or cursor layers per `UI-STACKING-CONTRACT`.
- **N5 (Console):** No uncaught errors attributable to `HeroTransition` / `ExpertiseTransition` / Lenis bridge during the UAT scroll path.
- **N6 (Build hygiene):** `pnpm run lint` and `pnpm run build` exit 0 on the closing commit.
- **N7 (Traceability):** `99-UAT.md` records outcome (PASS/FAIL), date, commit, and browser; ROADMAP Phase 99 marked complete only on PASS.
