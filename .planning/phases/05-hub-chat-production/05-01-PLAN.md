---
phase: 05-hub-chat-production
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/api/hub/chat/route.js, src/app/hub/chat/page.jsx]
autonomous: true
requirements: [HUB-PH2, HUB-PH5]
must_haves:
  truths:
    - "User can send a message and receive a streamed response at /hub/chat"
    - "API correctly handles UIMessage to CoreMessage conversion"
    - "Streamed response uses the x-vercel-ai-ui-message-stream header"
  artifacts:
    - path: "src/app/api/hub/chat/route.js"
      provides: "Streaming multi-provider AI route"
    - path: "src/app/hub/chat/page.jsx"
      provides: "Production Chat UI scaffolding"
  key_links:
    - from: "src/app/hub/chat/page.jsx"
      to: "src/app/api/hub/chat/route.js"
      via: "useChat hook POST request"
---

<objective>
Implement the streaming API foundation and the production chat UI scaffolding at /hub/chat.

Purpose: Move from placeholder state to a functional multi-provider streaming chat interface.
Output: Working POST route and client-side chat page.
</objective>

<execution_context>
@C:/Users/kevin/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kevin/.gemini/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/hub-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the POST /api/hub/chat route</name>
  <files>src/app/api/hub/chat/route.js</files>
  <behavior>
    - Accepts POST request with { messages, model, systemPrompt, enableWebSearch, activeSkills, activeMcps }
    - Uses convertToModelMessages(messages) to prepare payload for streamText
    - Resolves model via MODEL_REGISTRY and provider map
    - Implements stopWhen: stepCountIs(5) for multi-step tools
    - Returns result.toUIMessageStreamResponse()
    - Handles Google Search grounding tool conditionally (never mix with custom tools)
  </behavior>
  <action>
    Implement the route using the patterns established in hub-RESEARCH.md. Use "edge" runtime. Ensure all 4 Vercel AI SDK v6 bugs identified in research are fixed.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Route exists and returns 200/streamed response when called via curl or postman</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold /hub/chat/page.jsx</name>
  <files>src/app/hub/chat/page.jsx</files>
  <action>
    Create a "use client" component that integrates the useChat hook. Initialize body state with model, systemPrompt, and toggles. Implement a basic scrollable message list and input field using shadcn/ui primitives. Initialize state from localStorage where applicable.
  </action>
  <verify>
    <automated>ls src/app/hub/chat/page.jsx</automated>
  </verify>
  <done>Chat page is accessible at /hub/chat and can submit messages to the API</done>
</task>

</tasks>

<verification>
- [ ] curl -X POST http://localhost:3000/api/hub/chat -d '{"messages": [{"role": "user", "content": "hi"}]}' returns streamed text
- [ ] Navigation to /hub/chat loads the UI shell without errors
</verification>

<success_criteria>
- End-to-end streaming message flow from UI to API and back works for a basic text prompt.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hub-chat-production/05-01-SUMMARY.md`
</output>
