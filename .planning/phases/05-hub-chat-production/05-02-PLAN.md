---
phase: 05-hub-chat-production
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [src/app/hub/chat/page.jsx, src/components/hub/ChatControls.jsx]
autonomous: true
requirements: [HUB-PH5]
must_haves:
  truths:
    - "User can change models and toggles in the UI"
    - "Chat settings persist across page refresh via localStorage"
    - "Body payload sent to API reflects current UI settings"
  artifacts:
    - path: "src/components/hub/ChatControls.jsx"
      provides: "Unified chat settings and MCP status panel"
  key_links:
    - from: "src/app/hub/chat/page.jsx"
      to: "src/components/hub/ChatControls.jsx"
      via: "React props and state"
---

<objective>
Implement the interactive chat controls and state management for multi-provider support.

Purpose: Allow users to configure the AI session (model, search, skills) and ensure these settings persist.
Output: Integrated ChatControls component and persistence logic.
</objective>

<execution_context>
@C:/Users/kevin/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kevin/.gemini/templates/summary.md
</execution_context>

<context>
@.planning/phases/hub-RESEARCH.md
@src/app/hub/chat/page.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ChatControls UI component</name>
  <files>src/components/hub/ChatControls.jsx</files>
  <action>
    Create a component that renders:
    - Model selection dropdown (Gemini, Claude, GPT, DeepSeek, etc.)
    - Web Search toggle (Gemini Search Grounding)
    - MCP status list (context7, gemini-bridge, etc.)
    - Skill toggles (db-access)
    Use shadcn/ui Select, Switch, and Badge primitives. Follow the "lowercase" brand convention.
  </action>
  <verify>
    <automated>ls src/components/hub/ChatControls.jsx</automated>
  </verify>
  <done>ChatControls component exists and renders all required settings</done>
</task>

<task type="auto">
  <name>Task 2: Implement persistent settings and useChat body wiring</name>
  <files>src/app/hub/chat/page.jsx</files>
  <action>
    - Initialize model and toggle states from localStorage.
    - Wire the 'body' parameter of the useChat hook to include { model, enableWebSearch, activeSkills, activeMcps }.
    - Ensure settings are saved to localStorage on change.
    - Pass state and handlers to ChatControls component.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>UI settings correctly influence the chat requests and persist across refreshes</done>
</task>

</tasks>

<verification>
- [ ] Changing model in UI updates the request body in Network tab
- [ ] Toggling web search grounding correctly updates the 'enableWebSearch' payload
- [ ] Refreshing the page preserves selected model and toggles
</verification>

<success_criteria>
- The chat interface is fully configurable by the user, and configurations correctly reach the server.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hub-chat-production/05-02-SUMMARY.md`
</output>
