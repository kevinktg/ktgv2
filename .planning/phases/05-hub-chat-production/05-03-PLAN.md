---
phase: 05-hub-chat-production
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified: [src/app/hub/chat/page.jsx, src/app/api/hub/chat/route.js]
autonomous: true
requirements: [HUB-PH6, HUB-PH7]
must_haves:
  truths:
    - "Messages animate in smoothly using GSAP"
    - "Streaming text transitions are handled via GSAP for better visual flow"
    - "The db-access tool can be successfully called and results rendered"
  artifacts:
    - path: "src/app/hub/chat/page.jsx"
      provides: "Animated chat experience"
  key_links:
    - from: "src/app/api/hub/chat/route.js"
      to: "internal db-access skill"
      via: "tool execution in streamText"
---

<objective>
Refine the chat experience with GSAP animations and verify end-to-end tool calling.

Purpose: Provide a premium feel to the chat interface and ensure functional tools work in production.
Output: Animated message transitions and verified db-access tool integration.
</objective>

<execution_context>
@C:/Users/kevin/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kevin/.gemini/templates/summary.md
</execution_context>

<context>
@C:/Users/kevin/.agents/skills/gsap/SKILL.md
@src/app/hub/chat/page.jsx
@src/app/api/hub/chat/route.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate GSAP animations for message streaming and entry</name>
  <files>src/app/hub/chat/page.jsx</files>
  <action>
    Use @gsap/react useGSAP hook to animate:
    - New messages sliding/fading in (entry).
    - Streaming text updates (use a smooth reveal or opacity transition).
    - Ensure animations are cleaned up on unmount and performant (will-change: transform).
    Follow the "transform/opacity-first" rule from ROADMAP.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Chat messages have smooth entry and streaming animations</done>
</task>

<task type="auto">
  <name>Task 2: Verify and wire db-access tool integration</name>
  <files>src/app/api/hub/chat/route.js</files>
  <action>
    - Wire the 'db-access' tool into the buildTools function in the API route.
    - Implement the execute function for db-access to call the internal snippets API or DB directly.
    - Perform a live verification: ask the AI to "find snippets about GSAP" and confirm the tool call succeeds and results are returned.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Tool calling (db-access) is functional and verified in the live UI</done>
</task>

</tasks>

<verification>
- [ ] User sends a message and it fades/slides in via GSAP
- [ ] AI calls "db-access" tool and the UI shows the tool execution/result
- [ ] End-to-end conversation with tool results succeeds
</verification>

<success_criteria>
- The chat interface feels high-end (GSAP) and is fully functional as a tool-using agent gateway.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hub-chat-production/05-03-SUMMARY.md`
</output>
